// The messenger is the home page — open http://localhost:3000 and you're in the app,
// no /messenger path needed. The implementation lives in app/messenger/page.tsx and is
// re-exported here so both "/" and "/messenger" render it.
export { default } from "./messenger/page";
